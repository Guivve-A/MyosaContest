/* Puente al flatbuffer generado.
 *
 * El .c del modelo se emite en firmware/src/, fuera del sketch, porque es un
 * artefacto compartido y no codigo de este sketch. Arduino solo compila lo que
 * hay en la carpeta del sketch, asi que este archivo lo arrastra desde su sitio
 * en vez de copiarlo: dos copias del mismo flatbuffer es como el firmware acaba
 * ejecutando un modelo distinto del que se valido.
 */
#include "../../src/pneumocoach_model.c"
